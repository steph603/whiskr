<p id="notice"><%= notice %></p>


<%= image_tag @pet.picture, class:"pet-image"  if @pet.picture.attached? %>
<p>
  <strong>Name:</strong>
  <%= @pet.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @pet.user_id %>
</p>

<% if can? :manage, @pet %>
<%= link_to 'Edit', edit_pet_path(@pet) %> |
<%end%>
<%= link_to 'Add Another Pet', new_pet_path %>
<%= link_to 'Back', pets_path %>
